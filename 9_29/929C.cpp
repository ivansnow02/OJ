/**
 * 编程序，任意输入5个数据（其中第一个和第三个为整型数据，第二个和第四个为实型数据，第五个为一个字符型数据），
 * 求出它们的累加和sum，并将结果显示在屏幕上。
 *
 */

#include <bits/stdc++.h>
using namespace std;
int main()
{
	int a, c;
	double b, d;
	char e;
	cin >> a >> b >> c >> d >> e;
	cout<<a+b+c+d+e;
}
